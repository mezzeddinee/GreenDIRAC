[CIM]
# =========================================================
# CIM METRICS INGESTION API (AUTHENTICATED)
# OpenAPI: GreenDIGIT WP6 CIM Metrics API
# Base: /gd-cim-api/v1
# =========================================================

# Authentication
EMAIL = atsareg@in2p3.fr
PASSWORD = Green@31415

# CIM API base (token, verify-token, submit)
API_BASE = https://greendigit-cim.sztaki.hu/gd-cim-api/v1

# Metrics submission endpoint
METRICS_URL = https://greendigit-cim.sztaki.hu/gd-cim-api/v1/submit


[KPI]
# =========================================================
# KPI HELPER API (UNAUTHENTICATED)
# OpenAPI: GreenDIGIT KPI Service
# Base: /gd-kpi-api/v1
# =========================================================

# Used for:
#   POST /v1/pue
#   POST /v1/ci
#   POST /v1/transform-and-forward
API_BASE = https://greendigit-cim.sztaki.hu/gd-kpi-api/v1


[Defaults]
# =========================================================
# Fallback values if KPI calls fail
# =========================================================
PUE = 3
CI = 1000
ENERGY_WH = 8500


[Runtime]
# =========================================================
# Runtime behaviour
# =========================================================

# Token lifetime (hours) – matches CIM API (1 day)
TOKEN_MAX_AGE_H = 24

# Cache TTL for site KPI metrics (seconds)
CACHE_TTL = 300


